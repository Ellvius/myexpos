// Shell version - I

int main () {
    decl
        str filename;
        int temp;
    enddecl

    begin
        temp = exposcall("Write", -2, "Filename:");
        temp = exposcall("Read", -1, filename);
        temp = exposcall("Exec", filename);
        return 0;
    end
}